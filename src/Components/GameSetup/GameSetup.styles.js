import styled from 'styled-components';

const StyledGameSetup = styled.div`
    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
`;

export default StyledGameSetup;